Model에 데이터를 담을 때 model.addAttribute()메소드를 사용하는 두가지 방법이 있다.

1. Model addAttribute(String name, Object value)
   : value 객체를 name 이름으로 넣고, 뷰 코드에서는 name으로 지정한 이름을 통해서 value를 사용한다.

2. Model addAttribute(Object value)
   : value만 넣고, value의 패키지 이름을 제외한 단순 클래스 이름을 모델 이름(첫글자는 소문자)으로 사용한다. value가 배열이거나 컬렉션이면 
   첫 번째 원소의 클래스 이름 뒤에 List를 붙여 사용한다.
